import 'package:flutter/material.dart';

class BarItemPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Baritem')),
    );
  }
}
